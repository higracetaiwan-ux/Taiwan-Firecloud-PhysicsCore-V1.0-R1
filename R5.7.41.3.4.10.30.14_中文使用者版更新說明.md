# R5.7.41.3.4.10.30.14 中文使用者版更新說明

這一版沒有修改火燒雲判斷公式，也沒有把冰雲 Production 模式打開。

本版做的是一件 provenance 工作：確認 RRTM_SW v2.5 的關鍵舊程式，不只出現在 2020 的 external mirror。`tomflannaghan/pyrrtm` 在 2014 年就已保存一套 shortwave code；將它與 2020 v2.5 import 直接用 Git blob SHA 比較後，26 個 scientific-source files 中有 22 個為完全相同的 bytes。

其中最重要的 4 個檔案：
- `cldprop.f`
- `taumoldis.f`
- `k_gB24.f`
- `k_gB25.f`

在 2014 與 2020 兩條 repository histories 中全部 raw-byte identical。這讓目前使用的 Fu96 band tables / band 24 / band 25 歷史來源可信度再提高一層。

但這仍不代表我們拿到了 AER 當年的原始 `aer_rrtm_sw_v2.5.tar.gz`。兩個 repository 仍可能都是從同一份 AER 發布包衍生，所以 original tarball bytes、official hash、historical pre-averaging generator仍保持 Missing / blocked。

因此目前仍然：
- 不猜 Fu96 historical weights；
- 不由 final tables 反推出唯一 generator；
- 不開放 Production `tau_ice`；
- 不進 Step 3R。

下一步仍是尋找真正的 AER v2.5 archive bytes/hash，或直接找到 Q. Fu high-resolution pre-averaging source / generator。
