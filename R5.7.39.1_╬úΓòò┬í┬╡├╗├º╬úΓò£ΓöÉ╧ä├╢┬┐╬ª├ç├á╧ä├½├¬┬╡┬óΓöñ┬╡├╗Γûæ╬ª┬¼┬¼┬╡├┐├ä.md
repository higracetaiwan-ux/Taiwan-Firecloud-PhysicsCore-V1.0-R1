# R5.7.39.1 中文使用者版更新說明

這是一個小型但必要的 provider hotfix。

R5.7.39 新增的 GFS 中間壓力層 probe 本身邏輯正確，但真實 CASE 發現下載網址用錯：`pgrb2b` 是 GFS Secondary Parameters，必須走 `filter_gfs_0p25b.pl`，不能走主產品的 `filter_gfs_0p25.pl`。

R5.7.39.1 只修這個網址路由。它不會因為 probe 找到凝結物就直接宣布 COT READY，也不會改 Formation 分數或狀態。

測試：focused 10/10 PASS；完整 regression 552/552 PASS。
