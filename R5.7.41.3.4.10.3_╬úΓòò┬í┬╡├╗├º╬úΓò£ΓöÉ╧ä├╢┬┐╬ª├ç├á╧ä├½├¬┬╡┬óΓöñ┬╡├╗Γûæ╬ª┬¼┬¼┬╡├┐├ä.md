# R5.7.41.3.4.10.3 中文使用者版更新說明

這一版處理 Twilight Glow 的「觀測者降水路徑」耗時。

`.3.4.10.2` 在 TWS134 已把 Volume Assembly 從 25.414 秒降到 6.047 秒；新的最大 Glow component 變成 Observer Precipitation 14.309 秒。

`.10.3` 不改降水光學物理，而是把同一水平距離區間上不同 pressure level 原本重複計算的 17 點 Cloud→Observer 曲地球 LOS 共用一次。每一個垂直 hydrometeor layer 仍照舊逐層判斷是否交會、是否 Missing，以及累積多少 optical depth。

因此這是純 runtime 最佳化，不是降低解析度。
