# R5.7.41.3.4.4 中文使用者版更新說明

這一版主要是「跑得更有效率」，不是改火燒雲判斷規則。

近期 CASE 顯示 Twilight Glow 第三分支會反覆檢查同一條觀測方向上的雲層幾何。新版把完全相同、可安全重用的 cloud transect / COT lookup / conflict provenance / projected support geometry 做 process-local cache。

因此：

- 不會少算 550/575/600/650/700/750 nm；
- 不會跳過 Glow；
- 不會把 Missing 當成 0；
- 不會放寬 `DIRECT_EVIDENCE_CONFLICT`；
- 不會改 Formation、Viewing 或 Shadow COT。

另外，舊的 `AGGREGATION_AND_MATRIX_BUILD` 計時其實包含整段 Twilight Glow，因此看起來像「Glow 很慢 + Aggregation 也很慢」時，兩者有重疊。新版新增 `AGGREGATION_EXCLUDING_TWILIGHT_GLOW`，讓後續效能分析不再重複計算同一段時間。
