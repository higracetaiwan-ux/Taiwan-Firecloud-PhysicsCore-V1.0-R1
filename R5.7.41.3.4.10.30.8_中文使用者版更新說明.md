# R5.7.41.3.4.10.30.8 中文使用者版更新說明

這一版沒有改火燒雲 Formation、Viewing、霞光或六波段計算，而是把「RRTM_SW v2.5 到底有沒有殘存歷史程式」這個 provenance 問題往前推進。

目前已找到一份外部保存的 v2.5 source distribution。它保留了 AER 2004 年的 v2.5 更新說明、v2.5 makefile，以及帶 CVS revision/date/author 的 `cldprop.f`。與 AER 現在公開的 `cldprop.f` 比對後，2080 行只有 5 行 CVS 標記不同，其他科學程式與 table 內容一致。

同一份 v2.5 的 `taumoldis.f` 還明確區分 low-resolution 與 high-resolution Kurucz 太陽光譜。這代表目前 runtime 裡看到的 solar flux table 不能直接當作當年 Fu96 cloud-table averaging 的精確光譜權重。

因此，本版證明了 v2.5 的 source lineage，但沒有找到真正缺少的 Q. Fu high-resolution cloud table 或 band-averaging generator。Production Ice Optics 仍保持關閉。
