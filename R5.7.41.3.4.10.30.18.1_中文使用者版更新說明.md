# R5.7.41.3.4.10.30.18.1 中文使用者版更新說明

這版不是改火燒雲物理，而是修正 CAMS 已經在伺服器端執行、但本機等待時間到期後「沒有人回頭收結果」的問題。

`.10.30.18` 的合歡山北峰日出測試中，有四個 aerosol request 已經拿到 ADS request ID，也已經進入 running，但超過本機 running grace 後被標記 `TIMEOUT_DEFERRED`。原程式會保存 request ID，卻只等下一次整個分析重新執行才有機會 reattach，因此本次 CASE 仍缺 aerosol spectral payload。

`.10.30.18.1` 改成：同一分析 run 遇到這種情況時，等待短暫 cooldown 後，用「同一個 request ID」再連回 ADS 查狀態並下載結果。這個 recovery 模式禁止重新 submit；若原 request 已消失，就維持 Missing，而不是再送一份重複工作。

因此本版改善的是 CAMS 完整率與等待效率，不改 Formation、Viewing、Twilight Glow、六波段、Canvas、REZ、COT、冰雲光學或任何 frozen science 門檻。
