# R5.7.41.3.4.8 中文使用者版更新說明

這一版不是改火燒雲判斷公式，而是把「觀測者看到 Canvas 的視線幾何」算得更有效率。

上一版實跑顯示，Viewing + Photography 約 130.9 秒，其中單是 Viewing Path Geometry 就約 56.9 秒，是該區段最大瓶頸。程式以前會在很多 target 與視線高度中重複掃描相同 transect，也會對同一 blocker 重複做兩次 17 點視線取樣。

R5.7.41.3.4.8 把每條 transect 可重用的幾何先整理成固定計算計畫，並把同一次 17 點取樣重複利用。雲的位置、雲底/雲頂、Cloud Fraction、遮擋規則、Missing 規則、7 個視線高度以及 Earth curvature 都沒有改。

TWS106 的 1131 個 target 比對結果為 0 差異。本機測試約快 8 倍，但真正 Streamlit Cloud 能快多少仍要以 Field CASE 為準。
