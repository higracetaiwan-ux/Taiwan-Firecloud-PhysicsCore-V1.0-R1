# R5.7.41.3.4.10.9 中文使用者版更新說明

本版不改火燒雲判定，也不改 O2/H2O/O3 的吸收公式。

Main Viewing 在 `.10.4` 已會記住「同一份 LUT、同一氣體、同一波長、完全相同溫度與壓力」的吸收截面 sigma。但 Twilight Glow 在 Volume Assembly 還會把這些相同 spectroscopy state 再算一次。

`.10.9` 改成讓 Glow 直接共用 Main Viewing 已建立的 sigma cache。它只共用 cross section，不共用任何 target 的光學厚度或最終結果；每段 `sigma × density × path` 仍由 Glow 自己照原順序計算。

若 cache 或 LUT signature 不完整，就自動走原本 `_sigma_fast()`，不做近似、不跨 LUT 共用。

TWS091 9/14 日出同輸入離線測試：1092 個 Glow gas paths 0 差異，約 2.844 秒降至 1.073 秒（約 2.65×）。正式 Field speedup 仍需下一份 `.10.9` CASE 驗證。
