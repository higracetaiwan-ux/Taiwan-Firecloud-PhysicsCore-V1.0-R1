# R5.7.34 中文使用者版更新說明

這版修正 CAMS「90 秒到了就直接把本地 worker 殺掉」造成的假 timeout 問題。現在 ADS request 會保存 request ID，並把排隊與真正執行分開計時。若本地等待期限到了，遠端工作仍保留；下一次相同資料需求會先重新接上原 request ID，而不是立刻重送一份相同工作。

這版不改任何火燒雲科學權重，也不把 Missing 當成 0。
