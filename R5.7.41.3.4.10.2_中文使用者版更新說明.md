# R5.7.41.3.4.10.2 中文使用者版更新說明

這版繼續縮短 Twilight Glow 計算時間，但不減少任何物理計算層。`.3.4.10.1` 已把觀測者端 aerosol spectral extinction 明顯壓低，下一個最大戶變成 Glow Volume Assembly。

`.3.4.10.2` 將同一條 time/angle/direction route 的溫度、壓力與近地面分子邊界資料先整理成數值陣列。過去 1092 個 Glow 體積會反覆把同一份 pandas profile 轉成數值；現在每 route 只整理一次，再逐體積使用完全相同的 Rayleigh、HITRAN、10 m 邊界容忍與 1 m 量化規則。

TWS134 實際 CASE 1092 個 Glow volumes 已逐一核對：Rayleigh、local molecular state、boundary diagnostics 全部與舊算法完全一致。這仍是 Field-Test Candidate；下一個 CASE 要確認 `TWILIGHT_GLOW_COMPONENT_VOLUME_ASSEMBLY` 是否由約 25.4 秒明顯下降。
