# R5.7.41.3.4.10.23 中文使用者版更新說明

這一版首次把「Wyser 冰晶粒徑分布」與「Yang/Bi 單粒子消光截面」真正積分成六波段 bulk extinction 診斷值。

## 做到了什麼

程式現在可以在 diagnostic 模式計算：

```text
β_ext：每公尺的大氣消光係數
k_ext：每公斤冰水質量所對應的消光面積（m²/kg）
```

六個波段 550 / 575 / 600 / 650 / 700 / 750 nm 都完整計算，而且 18 組溫度 / IWC / numerical-grid 測試全部通過。

## 還沒有做什麼

這一版**仍然不會**把這些 `k_ext` 用到正式火燒雲預測：

- 不產生 production `tau_ice`。
- 不把 `single_column` 當成真實 runtime habit。
- 不把 `Rough000` 當成真實 runtime roughness。
- 不改 Formation、Viewing、Twilight Glow。

所以它是「bulk optics 計算器已能跑」，不是「Ice Optics 已正式上線」。

## 下一步

先跑 TWS091 + TWS100 FIELD CASE，確認 Step 3J evidence/gate/contract 能完整封存，而且實際 Ice runtime 仍保持 fail-close。通過後再進 habit / roughness / independent bulk validation 等 production certification 工作。
