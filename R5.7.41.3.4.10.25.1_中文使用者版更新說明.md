# R5.7.41.3.4.10.25.1 中文使用者版更新說明

這是一個**證據可重現性修正版本**，不是新的火燒雲物理功能。

`.10.25` 在 TWS091 實跑時，Step 3L 的 evidence 與 gate 都與正式 release 完全一致，但 contract 裡兩個非常小的 diagnostic 浮點尾數會因執行環境不同而改變。這不會改變 habit/roughness 判定，也不會改變任何火燒雲結果，但會讓正式 FIELD 證據無法做到 byte-exact。

`.10.25.1` 因此只把 contract 內的 diagnostic sample 數字做更穩定的固定化。科學計算仍維持原本精度，Formation、Viewing、Twilight Glow、六波段與所有 production gate 都不變。

目前狀態：**QA PASS / FIELD VALIDATION PENDING**。正式 FIELD baseline 仍是 `.10.24.1`，在 `.10.25.1` CASE 重跑通過以前不會進 Step 3M。
