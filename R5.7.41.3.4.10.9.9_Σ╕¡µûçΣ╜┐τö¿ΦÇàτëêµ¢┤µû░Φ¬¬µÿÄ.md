# R5.7.41.3.4.10.9.9 中文使用者版更新說明

這版繼續處理「程式跑太久」，不改火燒雲判斷規則。

`.10.9.8` 已把 DWD ICON 下載階段由約 159 秒降到 46 秒，證明 HTTPS 連線重用有效。但同一次 Field 也發現：程式雖標示「跨版本 user-level cache」，實際檔案仍存到 `.firecloud_state`，原因是主程式把自己的預設 state path 傳給 worker 後，provider 誤以為這是使用者明確指定的路徑。本版修正這個 cache contract。

CAMS 目前仍是主要耗時來源。O₃ 壓力層與 532 nm 3D 氣膠原本分成兩次 ADS 請求，但其時間、範圍、壓力層完全相同。本版先用一次「聯合壓力層請求」取得 O₃ + 532 nm 氣膠 + geopotential；資料完整才直接分給兩個原本的模組，資料不完整就退回舊方法。沒有用 RH、雲量或任何代理值補資料。

Field 驗證前仍標記為 `REGRESSION PASS / FIELD RETEST CANDIDATE`。
