# R5.7.41.3.4.10.24 中文使用者版更新說明

這一版的重點是：**開始用另一套方法檢查我們算出的冰雲消光係數是不是合理。**

上一版 Step 3J 是把 Wyser 冰晶粒徑分布與 Yang/Bi 單顆冰晶光學資料積分在一起，得到約 35 m²/kg 的 diagnostic `k_ext`。本版 Step 3K 不再使用 Yang/Bi `C_ext` 當 reference，而改用 Fu (1996) 的 geometric-optics projected-area 關係 `β≈2A_c` 做交叉檢查。

結果顯示兩條方法是同一量級，但仍相差約 26–31%。因此目前可以說「Step 3J 的數值不是完全沒有外部物理參照」，但**還不能說已經驗證到可以拿來產生正式 `τ_ice`**。

所以本版仍然：
- 不猜 Dmax。
- 不猜 habit。
- 不猜 roughness。
- 不把 diagnostic `k_ext` 寫入 production Ice runtime。
- 不計算 production `τ_ice`。
- 不改 Formation / Viewing / Twilight Glow。
