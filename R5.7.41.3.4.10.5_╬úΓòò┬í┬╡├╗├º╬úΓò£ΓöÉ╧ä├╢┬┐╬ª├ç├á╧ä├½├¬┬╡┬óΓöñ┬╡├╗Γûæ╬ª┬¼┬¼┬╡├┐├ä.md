# R5.7.41.3.4.10.5 中文使用者版更新說明

這一版繼續縮短 Viewing / Twilight Glow 的執行時間，但沒有改火燒雲物理規則。

`.10.4` 已在 TWS134 Field CASE 證實 Glow Observer Spectral Extinction 從 8.533 秒降至 6.642 秒，正式 FIELD PASS。

`.10.5` 發現程式雖然早已把 route 依時間／太陽角度／方向整理好，但每一個 Glow volume 還會重新把同一份 aerosol 與 gas DataFrame 切成「小於目標距離」的副本。實際積分器本來就會自己在目標距離停止，因此這些 pandas 切片只是重複成本。

新版直接重用已整理好的 route group，不減少資料點、不減少 pressure levels、不合併六波段，也不改 Missing 判定。

TWS134 同輸入離線測試：

- Glow 1092 targets：結果完全相同，runtime 約再降 15–20%。
- Main Viewing 585 targets：結果完全相同，runtime 亦下降。

正式 Field 改善幅度仍以部署 `.10.5` 後的新 CASE 為準。
