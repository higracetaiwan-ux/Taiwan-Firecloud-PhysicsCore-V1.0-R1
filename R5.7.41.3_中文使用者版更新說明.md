# R5.7.41.3 中文使用者版更新說明

這一版是正式開始累積 Shadow Mode 日出／日落 CASE 前的「收集準備版」。

主要改動：
- 新增觀測景點下拉選單，可先選北部／中部／南部／花東／離島，再搜尋景點名稱。
- 共用台灣晨昏攝影景點母資料庫 V2.2，共 187 個 GPS 景點。
- 選景點後自動帶入 GPS；仍保留「自訂座標」。
- 每個預設景點有固定 `site_id`，同一位置跨日期 CASE 可以可靠比對。
- CASE 會自動附上 Shadow Validation manifest、cohort summary、Ground Truth template 與 runtime summary。
- CASE 檔名加入 `site_id`，方便整理。
- 新增 baseline freeze guard，確保收集期間不會偷偷切換 Production COT 或讓 Shadow COT 進入 Formation。

這一版不會改變火燒雲 Formation、Viewing、Glow、Photography 或任何既有物理結果。
