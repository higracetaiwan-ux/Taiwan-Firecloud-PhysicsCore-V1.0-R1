# R5.7.41.3.4.10.30.5 中文使用者版更新說明

這版沒有開啟新的冰雲 production 計算。它做的是把一個容易誤判的 provenance 問題正式鎖住：AER 公開的 RRTM_SW 程式裡，Fu96 的資料已經是「平均後的 band table」，不是當年 Q. Fu 的高光譜原始資料，也不是產生那些 table 的歷史 generator。

因此現在程式會明確禁止：從 `EXTICE3/SSAICE3/ASYICE3` 最終表格倒推唯一的 h、solar grid 或 weights。這可以避免為了趕進度而把不唯一的反問題當成歷史真值。

目前仍維持 fail-close，Production Ice Optics 不解鎖。
