# R5.7.41.3.4.10.7 中文使用者版更新說明

這一版不改火燒雲判定，也不減少任何降水垂直層或 LOS sampling。

上一版 Main Viewing 與 Twilight Glow 會各自把同一組 GFS RWMR/SNMR/GRLE 路徑資料重新整理一次。`.10.7` 改成 Main Viewing 整理一次後，Glow 直接共用這份不可變的 3-D hydrometeor context。

Glow 仍然會針對自己的每一個 scattering volume 個別計算 Cloud→Observer 路徑，所以不是把 Main Viewing 的結果直接複製給 Glow；只共用前處理資料。

目前本版已通過完整 regression，但仍需實際 CASE 驗證 Observer Precipitation 是否由 `.10.6` 的約 8.25 秒明顯下降，因此狀態是 FIELD TEST CANDIDATE。
