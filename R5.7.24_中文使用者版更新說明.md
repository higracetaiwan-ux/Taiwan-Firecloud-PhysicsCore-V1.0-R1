# Taiwan Firecloud PhysicsCore V1.0-R5.7.24｜中文使用者版更新說明

這一版不是「讓程式跑得更快」的版本，而是先解決更重要的事情：**讓長時間火燒雲分析更有機會穩定跑完。**

R5.7.23.4 已經成功完成過一次真正的 Cold Test，但實測 RAM 會由約 786 MB 一路增加到約 1 GB，最高超過 1.1 GB；過去也曾發生程式自行停止、刷新後沒有 recovery 資訊。因此 R5.7.24 把記憶體與背景 worker 穩定性列為第一優先。

主要改變：

- 13 個時間點的完整預報 snapshot 不再全部同時塞在 RAM。
- 每個角度的大型雲體、氣體、氣膠、光譜資料算完後先放到本機暫存，需要彙整時才取回。
- UI 只保留真正需要顯示的少量資料，不再把完整 provider snapshot 留 13 份。
- 每個角度完成後主動做垃圾回收與 Linux 記憶體 trim。
- 13 angles 全部完成後，先釋放已經用完的 GFS/CAMS/DWD in-memory cache，再開始最後矩陣彙整。
- Streamlit 頁面不再用一個長時間迴圈卡著等分析；背景 worker 繼續跑，頁面只做短時間狀態更新。
- 刷新頁面時若 worker 還活著，會重新連線監看，而不是再開第二個分析。
- Recovery 狀態有多份 journal；即使主 journal 損壞，仍嘗試從 job/attempt 目錄重建。

這些改動**沒有降低科學解析度**：仍保留 13 angles、0.5 km 垂直格點、六波段、Formation / Viewing 分離、Missing 規則與完整 CASE 證據。

下一次驗收仍建議先跑 `COLD_ISOLATED_TEST`。這次最重要的不是跑幾分鐘，而是：

1. 能否一次跑完整；
2. RAM 是否不再一路逼近 1.1 GB；
3. 即使 CAMS timeout，是否仍能以 Missing/Partial 狀態完成；
4. 若頁面刷新，是否能重新連線原 worker；
5. CASE 的 Formation / Viewing / Tier-2 核心結果是否與 R5.7.23.4 一致。
