# R5.7.41.3.4.10.26 中文使用者版更新說明

這一版不是把冰雲光學直接投入火燒雲 production，而是把前一階段一個「比較方法不完全公平」的問題拆乾淨。

Step 3K 比較 Fu96 與 Yang/Bi 時，兩邊用了不同的冰晶幾何，所以 26–31% 的差距不能全部算成光學模型本身的差距。Step 3M 改成兩邊使用同一套 Yang/Bi `single_column` Dmax 與投影面積，再用 Fu96 的 `Cext≈2A` 做獨立 extinction reference。

同幾何後，bulk 最大差距降到約 **2.88%**、平均約 **0.59%**。這支持先前的大差距有相當部分來自幾何 mismatch，但不能解讀成「冰雲光學已全部驗證完成」，也不能把 3% 當成新的 production 門檻。

目前只有 extinction reference 可進一步使用於 diagnostic qualification；SSA、散射不對稱因子 g、GFS 真實 habit/roughness、`tau_ice` 與 production Ice Optics 仍全部鎖住。Formation / Viewing / Twilight Glow 與火燒雲 Frozen Science 完全沒有改。

目前狀態：**QA PASS / FIELD VALIDATION PENDING**。正式 FIELD baseline 是 `.10.25.1 FIELD PASS`；必須先跑 `.10.26` FIELD CASE，才能決定 Step 3M 是否 FIELD PASS。
