# R5.7.41.3.4.10.16 中文使用者版更新說明

這一版的重點不是「終於找到一個 Dmax 公式」，而是確認一件更重要的事：**GFS v16 的 ice effective radius 不能直接當 Yang/Bi Dmax。**

GFDL/Wyser 的 `rei` 描述一整群冰晶的 radiative effective size；Yang/Bi `Dmax` 則是單一冰晶幾何最大尺寸，並且還要指定 habit 與 roughness。因此直接把 `rei`、`2×rei` 或 effective diameter 塞入 Dmax 軸會混淆物理語義，本版已正式禁止。

PhysicsCore 找到的下一條合理路線，是先重建 Wyser 的冰晶粒徑分布與 hexagonal-column 幾何，再使用 Yang/Bi 的 Dmax 單粒子資料做 PSD 積分，得到 bulk 六波段 Ice Optics。這條路目前只是候選，尚未啟用。

所以本版仍維持：
- Dmax mapping OFF
- habit mapping OFF
- roughness mapping OFF
- Ice τ production synthesis OFF
- WINDY production Ice bundle promotion OFF

這保證目前的 WINDY 外掛不會收到一份「看起來完整但 Dmax 是猜出來」的冰雲光學資料。
