# R5.7.32 中文使用者版更新說明

這版修正的不是新權重，而是 CAMS 高度單位。R5.7.31 真實 CASE 顯示 CAMS `z` 以 `m**2 s**-2` 回傳，但舊程式沒有辨識這種 ecCodes 拼法，造成 pressure-level 高度被放大約 9.8 倍，進而讓 60–100 km Glow→Observer aerosol path 大量變成 Partial。

R5.7.32 會先把 geopotential 除以 9.80665 轉成正確的 geopotential height。若單位無法辨識，資料維持 Missing，不猜。

修正後，舊 CASE 的離線重播顯示 60/80/100 km 由大量 Partial 恢復；最後 6 個 100 km target 只差約 5 m 低於最低原生 1000 hPa 層，因此 Glow 專用允許 <=50 m 的最低 native level endpoint snap。一般 Viewing 不啟用這個容差。

本版不加入 aerosol SSA、phase function 或 multiple scattering，也不修改 Formation / Viewing / Photography。
