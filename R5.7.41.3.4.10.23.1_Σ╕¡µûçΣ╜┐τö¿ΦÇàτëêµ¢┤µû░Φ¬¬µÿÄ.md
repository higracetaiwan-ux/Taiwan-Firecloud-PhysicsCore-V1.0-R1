# V1.0-R5.7.41.3.4.10.23.1 中文使用者版更新說明

這是一個**證據封存與可重現性 hotfix**，不是新的火燒雲物理版本。

`.10.23 TWS100` 實跑時，CAMS 壓力層 bundle 已因 ADS queue grace 超時而進入 `TIMEOUT_DEFERRED`，後續 O₃ / aerosol fallback 也成功，因此科學結果正常；但 CASE 的 worker checkpoint 還停在 `RUNNING`。`.10.23.1` 修正這個矛盾，讓 CASE 封存能正確顯示該 worker 已進入 terminal timeout 狀態。

另外，Step 3J diagnostic evidence 的浮點輸出固定精度，避免不同執行平台只因最後一個浮點 bit 不同而讓 CSV 無法 byte-identical。

**不變的部分：** Formation、Viewing、Twilight Glow、六波段、Wyser/Yang 計算、Ice runtime、Production Ice Optics gate 全部不變。
