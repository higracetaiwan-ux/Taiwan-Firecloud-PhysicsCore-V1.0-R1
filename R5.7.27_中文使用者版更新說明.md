# R5.7.27 中文使用者版更新說明

這一版修正「明明沒有形成火燒雲，Viewing 卻讓最後攝影結果顯示 FAIR」的問題。

現在程式一定先看 Formation：

- 如果沒有有效 Canvas，最後就是 `NO_GO`。
- 如果 Canvas 全部進入 Earth Shadow，最後也是 `NO_GO`。
- Viewing 仍會告訴你觀測方向是否有遮擋，但它不能把「沒有形成」改成「可以拍」。

Photography Decision 也不再只顯示有 Viewing target 的少數角度，而是跟 Formation 一樣完整顯示 0°～−6°的 13 個角度。

以 2026-09-08 sunset CASE 為例，舊版只有 −5.5°、−6°兩列，而且顯示 FAIR；新版會完整顯示 13 列，所有 Formation 已確定 NO-GO 的角度都正確顯示 `NO_GO`。
