# R5.7.41.3.4.6 中文使用者版更新說明

這版不改火燒雲物理判斷，重點是讓同一批資料「不要一直重下載、不要重做無意義 I/O」，並把剩餘耗時拆清楚。

## DWD 同一預報資料可以真正跨分析重用

正常分析時，DWD ICON 的原始 model-level GRIB 會保存到共用 persistent cache。只要 model、產品、網格、run、forecast lead、變數、model level 全部完全相同，下一次分析可以直接讀本地 raw cache。

不是只看檔名存在就使用：程式會檢查 exact identity、cache stamp、byte size 與 SHA256。任何不一致或檔案損壞都會重新下載，不會把錯誤資料帶進光學計算。

Cold Test 仍會隔離事件資料，因此仍能測真正的 cold-run；DWD 靜態 remap 資源則不需要重下載。

## DWD API 統計修正

前一版真實 CASE 中，DWD 實際有 176 次下載，但 API efficiency table 顯示 0。原因是成功解碼後 status 變成 `OK_DOWNLOADED`，舊 audit 只認 `DOWNLOADED`。

本版改成直接記錄 network_requested / success / failure / bytes，因此可以正確分辨：

- 真正網路下載；
- persistent raw cache hit；
- process-local decoded-field cache hit；
- all-404 negative availability cache hit。

## CASE ZIP 也一起最佳化

大型 CSV 仍是 streaming，不會先在記憶體產生整份 CSV；本版把 pandas 很多小 write 合併成最多 4 MiB 再送進 ZIP。H004 三個大型 CSV 測試約快 7.36%，檔案內容與 hash 完全一致。

## 把 aggregation 167 秒拆開看

本版先不冒險改 aggregation 演算法，而是把它拆成多個獨立計時：Cloud matrix、Spectral/Atmos matrix、Formation、Viewing/Photography、Tier-2、Completeness 等。下一版 R5.7.41.3.4.7 就能直接對最大 hotspot 下手，不再猜。

## 不變的部分

Shadow、COT、Earth Shadow、Formation、Viewing、Twilight Glow、Photography、六波段、Missing 規則全部不變。這是一個 runtime / I-O hardening 版本。
