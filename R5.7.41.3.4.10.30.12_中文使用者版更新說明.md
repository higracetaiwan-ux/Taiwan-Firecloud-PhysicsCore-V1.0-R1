# R5.7.41.3.4.10.30.12 中文使用者版更新說明

這一版不是改火燒雲判斷公式，而是繼續追查 **RRTM_SW v2.5 原始資料從哪裡來、怎麼驗證**。

這次多確認了兩件重要事情：

第一，AER 官方頁面目前仍把 `aer_rrtm_sw_v2.5.tar.gz` 指向自己的 `files.aer.com` 下載端點，因此我們現在知道正式 binary endpoint，不再只有檔名。

第二，歷史 v2.5 更新說明仍記錄當年的 anonymous FTP 路徑；另外有獨立安裝紀錄顯示，從 AER 下載並解壓後可看到 `src`、`makefiles`、說明檔與 `update_rrtm_sw_v2.5.txt`，makefile 也標示 `VERSION = v2.5`。這能幫助我們判斷未來取得的 tarball 是否有合理的 distribution footprint。

本版另外加入一個檢查工具：
`tools/verify_aer_rrtm_sw_v25_archive.py`

真正取得 tar.gz 後，它可以立即算 SHA256 / MD5、列出 archive manifest 並檢查重要檔案是否存在。

但是目前 ChatGPT 的 binary 取得環境對 AER 檔案下載 timeout，所以**這一版仍不能宣稱已經拿到 original tarball bytes，也不能宣稱已有官方 hash**。因此 Production Ice Optics 不解鎖，Step 3R 也不進入。
