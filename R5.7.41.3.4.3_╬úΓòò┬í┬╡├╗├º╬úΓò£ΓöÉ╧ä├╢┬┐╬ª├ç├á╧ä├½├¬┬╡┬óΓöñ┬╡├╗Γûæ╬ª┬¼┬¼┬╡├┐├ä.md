# R5.7.41.3.4.3 中文使用者版更新說明

這版不改火燒雲物理規則，主要是讓程式不要在 DWD ICON secondary provider 上做大量重複工作。

近期歷史 CASE 顯示，同一個 DWD run/lead 因為 13 個太陽角度被重複查詢；當歷史檔案已不在 DWD OpenData 時，單次 CASE 會出現 1404 筆 HTTP 404。

R5.7.41.3.4.3 之後：

- 第一次完整查詢若證明所有 QC/QI 欄位都是 404，後面角度直接保留 Missing，不再重送相同請求；
- DWD 已成功下載／解碼的 QC/QI/T/P native fields，可在相同 run/lead 與相同路徑座標間重用；
- 各時間的 surface pressure anchor 與垂直高度仍重新計算，所以不會把某個時間的雲高直接複製到另一個時間；
- 任何 mixed failure、partial ready、decode error 都不會被錯誤判成永久 unavailable。

因此這是速度與 API reliability 改善，不是科學門檻改版。
