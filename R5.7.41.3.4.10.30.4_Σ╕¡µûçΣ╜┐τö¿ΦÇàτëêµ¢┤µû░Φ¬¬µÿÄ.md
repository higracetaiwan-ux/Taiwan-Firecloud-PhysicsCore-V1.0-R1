# R5.7.41.3.4.10.30.4 中文使用者版更新說明

這一版沒有開啟新的冰雲 Production 計算，而是把 Fu96 歷史平均法的 `h` 適用區域再縮小。

已確認 Fu-lineage 文獻中：0.7 µm 以下屬弱吸收區，`h=1`；0.7–1.22 µm 使用 `h=2/3`。因此 RRTMG band 25 全部落在 `h=1` 區域；但 band 24 剛好跨過 0.7 µm 邊界，所以不能直接給 band 24 一個單一 `h` 值。

這仍不是 archived RRTMG Fu96 tables 的 exact historical generator。程式繼續保持 fail-close，不會因為這些文獻約束就產生 production `tau_ice`、SSA 或 g。
