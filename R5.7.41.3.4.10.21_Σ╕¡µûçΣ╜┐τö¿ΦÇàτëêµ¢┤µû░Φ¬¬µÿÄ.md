# R5.7.41.3.4.10.21 中文使用者版更新說明

這一版完成的是 Ice Optics 的 **尺寸座標橋接**，不是把冰雲光學直接打開。

現在可以確定：Wyser PSD 使用的 `L` 與 Yang/Bi LUT 的 `maximum_dimension_um` 都是在描述「粒子的最大尺寸」，所以在 **size coordinate** 上可以對應。

但是這不代表兩邊的六角柱形狀一樣。本版另外利用 Yang/Bi V2 authoritative source 產生的 189 個 single-column geometry rows 重新驗證，確認目前 LUT 的 column geometry 對應 `a=0.35L`（小粒子）與 `a=3.48√L`（大粒子）。這套 shape law 與 Wyser Eq.(5) 不相同，因此不能因為 `L=Dmax` 就直接把 Wyser PSD 丟進 Yang/Bi LUT 算 production Ice τ。

目前正式狀態：

```text
L → Yang/Bi Dmax coordinate identity     PASS
Yang/Bi V2 column geometry source check  PASS
Wyser ↔ Yang shape compatibility         BLOCKED
Projected-area compatibility             BLOCKED
Volume / mass compatibility              BLOCKED
Eq.(6) independent corroboration         BLOCKED
Habit / roughness                        BLOCKED
Production Ice Optics                    BLOCKED
```

Frozen Firecloud Physics 完全不變；WINDY 的 validated Dmax mapping 仍保持 fail-close。
