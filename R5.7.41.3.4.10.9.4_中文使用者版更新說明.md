# R5.7.41.3.4.10.9.4 中文使用者版更新說明

這版新增「觀測者環境時間軸」。

以前 `.10.9.3` 已能在 NO_CANVAS 時保留 0–100 km 低雲環境，但時間只跟著 0°→−6°核心事件。現在增加 T−60 到 T+30、每 5 分鐘的診斷時間軸，讓日落前後實景可以直接和模式資料對照。

重要：

- 這不是新增火燒雲計算角度；
- −6°後只做環境診斷；
- 不用低雲量補造 COT/τ；
- native 3D 不做時間插值；
- 不因照片看到低雲就反向修改 Formation。

TWS106 離線測試已能對齊 17:07、17:52、18:02、18:15、18:26、18:29 的實景時間。
