# Taiwan Firecloud PhysicsCore V1.0-R5.7.41.3.4.10.25｜中文使用者版更新說明

這一版處理的是「冰晶到底是哪一種形狀、表面有多粗糙」這個 production 前的重要問題。

目前 GFS 只能提供冰雲凝結水等 bulk fields，**沒有直接提供 Yang/Bi habit 或 roughness**。因此 PhysicsCore 不會把 `single_column`、`Rough000`、`Rough003` 或 `Rough050` 偷偷當成真實預報值。

本版確認：
- Wyser solid-column lineage 可以與 Yang/Bi `single_column` 建立模型家族層級的橋接。
- 但兩套 column geometry 仍不是完全相同。
- roughness 三態都必須保留成 uncertainty ensemble。
- habit 與 roughness 都會實質影響 extinction / g，因此不能忽略。

正式 `tau_ice`、production Ice Optics 與 Formation promotion **仍未開啟**。
