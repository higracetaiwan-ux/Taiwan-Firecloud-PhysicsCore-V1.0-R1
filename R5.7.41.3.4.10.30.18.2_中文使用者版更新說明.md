# R5.7.41.3.4.10.30.18.2 中文使用者版更新說明

這版修正的是上一版 FIELD 測試才暴露出的 CAMS 真實執行路徑問題。

`.18.1` 原本已加入「同一 request ID 重新連線收結果」，但它只加在 serial scheduler；合歡山北峰真實 WARM_PRODUCTION 使用的是 adaptive whole-route scheduler，所以四個 aerosol request 在 ADS 還停在 `accepted` queue、超過 75 秒後仍直接結束成 `TIMEOUT_DEFERRED`。

`.18.2` 已把 same-request-ID reattach 加到真正 production adaptive path：
- queue timeout 可以 reattach；
- running timeout 也可以 reattach；
- 只接回原本 request ID；
- 禁止重新 submit；
- 若仍未完成就保持 Missing；
- timeout 不會再切成多個 geographic child requests。

這次修正完全不改火燒雲 Formation / Viewing / Twilight Glow、六波段、Canvas、COT 或 Ice Optics 科學規則。
