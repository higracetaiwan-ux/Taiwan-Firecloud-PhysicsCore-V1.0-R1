# R5.7.41.3.4.10.12.1 中文使用者版更新說明

這一版不是改火燒雲判斷，也不是新增冰晶粒徑公式；它修正的是「證據檔是否真的被完整保存與驗證」的保險機制。

`.10.12` 實跑已經能產生冰雲微物理能力盤點、Dmax/PSD mapping eligibility、Phase 2 contract 三個檔案，但 CASE Integrity 還沒有把它們列入「少一個就必須 FAIL」的清單。本版把這個缺口補上。

因此未來若這三個檔案任何一個因程式退化而沒有產出，Analysis Integrity / CASE Integrity 會直接顯示失敗，不會再出現「CASE 看起來 PASS，但 Phase 2 證據其實缺檔」的情況。

Frozen Science 完全不變；目前仍不允許用 IWP、溫度、RH、雲量或 r_eff 猜 Dmax，也不允許預設 habit、roughness 或 PSD。
