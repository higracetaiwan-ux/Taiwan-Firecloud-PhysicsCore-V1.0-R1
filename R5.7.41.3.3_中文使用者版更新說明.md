# R5.7.41.3.3 中文使用者版更新說明

這版專門修正「歷史回測跑到一半出現 `KeyError: direction_offset_deg`」的問題。

舊版遇到某個歷史時刻沒有可用 cloud-layer rows 時，可能拿到一張完全空、甚至沒有欄位名稱的表格；程式卻仍嘗試讀取 `direction_offset_deg`，因此整個 analysis worker 被中止。

新版處理方式：
- 歷史 cloud volume 缺資料 → 顯示 Missing / `VIEW_CLOUD_VOLUME_UNRESOLVED`，繼續分析。
- 不會把 Missing 當晴空。
- 如果資料表本身完整，只是該方向真的沒有 blocker，仍正常判定路徑晴空。

不會更改火燒雲 Formation、COT、Viewing、Glow 的科學權重或門檻。
