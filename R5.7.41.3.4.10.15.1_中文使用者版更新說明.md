# R5.7.41.3.4.10.15.1 中文使用者版更新說明

這一版不是改火燒雲物理，而是修 CASE 證據封存。

`.10.15` 的合歡山北峰 FIELD CASE 顯示一個特殊問題：程式分析階段知道 GFS v16 Step 3B 有 8 筆 scheme-pin evidence，也知道 gate 與 contract；但是下載出的 CASE ZIP 裡，那三個檔案卻變成空檔。舊的 CASE Integrity 只確認「有這個檔名」，所以仍顯示 PASS。

`.10.15.1` 改成：
- CASE 建檔時直接從目前執行版本重新建立 Step 3B 靜態 evidence；
- 不再只相信 session/result handoff；
- CASE Integrity 除了檢查檔名，也檢查內容：
  - evidence 至少 8 rows；
  - gate 至少 1 row；
  - contract 不能是空 `{}`。

若未達成，CASE Integrity 會直接 FAIL。

這版沒有把 effective radius 轉成 Dmax，也沒有新增 PSD、habit 或 roughness 假設。Frozen Science 完全不變。
