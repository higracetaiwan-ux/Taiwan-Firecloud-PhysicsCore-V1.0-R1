# R5.7.37 中文使用者版更新說明

R5.7.37 解決的是一個非常小、但物理上不能用「放寬容忍值」處理的近地層缺口。

先前台灣 CASE 在太陽高度 −6°、100 km、3.75 km Glow scatter volume 時，觀測者視線第一個取樣點約在 75 m 高度，但當時最低 pressure-level 約在 96 m，因此差了約 21 m。舊規則只允許 10 m 內的最低端點 snap，所以程式正確標成 Partial。

本版沒有把 10 m 改成 25 m，而是增加一個真正的近地大氣 anchor：CAMS O₃ model level 137 約在 10 m 高度，並搭配地表氣壓、2 m 溫度與 2 m 相對濕度建立近地分子狀態。如此 75 m 會落在「約 10 m 的 ML137」和「約 96 m 的 1000 hPa」之間，變成真實內插，而不是向地面外插。

如果 ML137 O₃ 或 surface T/RH/pressure 任一資料拿不到，程式仍會保留 Missing，不會用固定值補洞。

### 使用者需要注意

安裝 R5.7.37 後需要重新跑一個新的 CASE。R5.7.36 舊 CASE 沒有新資料，不能拿舊 CASE 直接判定 R5.7.37 field-pass。

### 發行驗證

- Working tree regression：**533/533 PASS**。
- FULL-CLEAN 解壓後 regression：**533/533 PASS**。
- 正式封包：**506 個檔案成員，0 cache/pyc 污染**。
- 程式與封包已可部署；科學 field validation 仍必須以 R5.7.37 新 CASE 驗證。
