# R5.7.35 中文使用者版更新說明

這一版正式加入 Twilight Glow 的「氣膠單次散射」物理。

程式會從 CAMS 取得原生 AOD、單次散射反照率 SSA 與 asymmetry factor g，搭配原本已建立的 native 3D aerosol extinction 532 nm，計算 550/575/600/650/700/750 nm 六波段的氣膠散射來源。

重點：

- 不使用固定 SSA。
- 不使用固定 g。
- 不使用固定 Angstrom 指數。
- 不以 column AOD 假造 3D 垂直分布。
- 非 CAMS 原生波長只做兩個原生波長之間的有界內插，不外插。
- phase function 使用 CAMS g 驅動的 Henyey-Greenstein 近似，會明確標示為近似。
- native 3D extinction / AOD532 / SSA / g 任一必要證據缺失時保持 Missing。
- 新增 `v1_twilight_glow_aerosol_scattering_550_750nm.csv`。

這一版仍不是絕對天空輻亮度。Multiple scattering 與絕對 radiometric calibration 尚未完成，因此 `calibrated_glow_radiance_available` 仍為 False。
