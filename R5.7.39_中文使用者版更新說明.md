# R5.7.39 中文使用者版更新說明

這一版要查的是目前 Canvas Optical Truth 最核心的一個疑點：GFS 主壓力層資料顯示「有雲量，但原生凝結水/冰為 0」，究竟是真的模型衝突，還是因為高空只取 100/150/200 hPa 等較粗的壓力層，剛好漏掉夾在中間的薄高雲凝結物。

R5.7.39 新增 GFS `pgrb2b.0p25` 中間壓力層探針，會讀取 125、175、225、275…925 hPa 的原生 CLWMR、ICMR、TCDC、TMP、HGT，並只保留真正落在 0–100 km Formation Canvas 垂直範圍內的資料。

這一版**不會因為探針找到冰水或液水，就直接把 COT 判定為可用，也不會改 Formation 結果**。目的先是取得證據，避免為了讓 `UNCERTAIN_OPTICS` 消失而把模型間不同高度的雲硬湊在一起。

如果下一個 CASE 發現：主層 150 hPa 的 condensate 是 0，但同一 Canvas 的 125 或 175 hPa 有原生正 ICMR/CLWMR，就能證明原本至少部分 conflict 是垂直取樣不足造成；之後才有物理依據設計 Phase 2。

如果中間層也都是原生 0，那 conflict 就應繼續保留，不能硬補 COT。

本版仍遵守：Missing 不等於 Clear、RH/cloud cover 不得假造 COD/COT、Formation/Viewing/Glow 分離、六波段不變。

## 程式驗證

- R5.7.39 focused tests：**9/9 PASS**。
- 完整 working-tree regression：**551/551 PASS**。
- Field Validation：需要 R5.7.39 新 CASE。
