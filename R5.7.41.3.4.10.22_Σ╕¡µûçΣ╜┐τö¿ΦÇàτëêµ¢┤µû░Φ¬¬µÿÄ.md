# R5.7.41.3.4.10.22 中文使用者版更新說明

這一版的重點不是「Ice Optics 已能正式算 τ」，而是把 **Wyser 的冰晶族群** 與 **Yang/Bi 的單粒子光學 kernel** 接到同一個已驗證 Dmax 座標上，而且明確避免把兩套質量定義混在一起。

## 這次真正完成的事

1. Wyser `L` 與 Yang/Bi `maximum_dimension_um` 繼續沿用 Step 3H 的 coordinate PASS。
2. Yang/Bi V2 `single_column/Rough000` 可以重建六波段單粒子 `C_ext`：109 個粒徑、654 個光譜 rows。
3. Wyser Eq.(6) mass 與 Yang geometric mass 被正式分成兩個不同用途，不准互換。
4. 已證明同 Dmax 下，兩套 column 的 projected area / volume / mass 並不相同，因此不會偷做 hidden correction。

## 還沒有開放的功能

- 不會從 GFS 猜 Dmax。
- 不會把 `single_column` 當成 production habit。
- 不會把 `Rough000` 當成 production roughness。
- 不會產生 production Ice `tau_ice`。
- 不會因 Ice development 改 Formation 判定。

所以目前最重要的意思是：

> **數值橋已能跑，但科學資格仍保持 fail-close。**

最新正式 FIELD baseline 是 `.10.21 FIELD PASS`；`.10.22` 還需要實跑 CASE 驗證 evidence/gate/contract 是否完整封存。
