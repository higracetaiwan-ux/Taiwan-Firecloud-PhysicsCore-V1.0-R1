# R5.7.31 中文使用者版更新說明

這一版不是重新設計火燒雲判斷，而是把「霞光」第三分支的兩段光路拆開算清楚。

以前 R5.7.30 已經能建立：

`太陽 → 大氣散射體積 → 觀測者`

但 Sun path 與 Observer path 的六波段 extinction 主要封裝在既有 evidence 裡。
R5.7.31 現在把兩段都明確輸出成獨立 CASE 表，逐波段列出：

- Rayleigh；
- O2 + H2O；
- O3；
- aerosol；
- cloud；
- precipitation；
- total optical depth；
- transmission。

其中 575 nm 的 O3 Chappuis absorption 會獨立保留，但不會重複加兩次。

只要任何一個 component 缺資料、Partial 或 Evidence Conflict，就不會硬算完整
`tau_total`、transmission 或最終霞光 proxy。Missing 不會被當成 0。

新增三個主要 CASE 檔：

- `v1_twilight_glow_sun_to_scatter_extinction_550_750nm.csv`
- `v1_twilight_glow_scatter_to_observer_extinction_550_750nm.csv`
- `v1_twilight_glow_single_scattering_550_750nm.csv`

目前結果仍是「相對的單次 Rayleigh 散射六波段 proxy」，不是校準完成的天空亮度。
Aerosol 散射相函數、SSA、多重散射與絕對輻射校正仍留待後續版本。

Formation、Viewing、Photography Decision、13 個太陽角度、六波段與既有權重都沒有
修改。
