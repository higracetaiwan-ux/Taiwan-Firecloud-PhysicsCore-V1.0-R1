# R5.7.41.3.4.10.28 中文使用者版更新說明

這一版開始真正「把 Yang/Bi 算出的冰雲散射特性，拿去跟獨立 Fu96/RRTMG 數值表比較」，但比較仍屬診斷，不會直接改變火燒雲預測。

重點是：RRTMG 的 SSA 與 g 是寬頻帶平均值，而 PhysicsCore 目前 Yang/Bi portable LUT 只有 550、575、600、650、700、750 nm 六個單色點。兩者不是完全同一種光譜資料，所以本版可以回答「數值差多少、是否穩定、是否落在有效尺寸域」，但不能說「六波段已被獨立驗證」。

本次 54 個 comparison rows 全部落在 Fu96 有效 Dge 範圍。SSA 差異非常小，g 的最大絕對差約 0.0215。這些數值只作工程與科學診斷，不是新的門檻。

因此 Production Ice Optics、`tau_ice` 與 Formation 評分仍完全不變。
