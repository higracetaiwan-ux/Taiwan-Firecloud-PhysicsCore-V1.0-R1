# R5.7.41.3.4.10.20.1 中文使用者版更新說明

這一版把 Step 3G 最重要的兩條 Wyser 原始數學式真正恢復到可運算狀態，但仍維持「先驗證、後上線」。

最重要的修正是：先前常見的 `D=2.5 L^0.6` 不再被當成 single-author Wyser (1998) Eq.(5)。真正恢復的 Eq.(5) 是 30 µm 前後的分段長寬比。Eq.(6) 則恢復為 `m_g=2.311e-2*(L_um/1e4)^2.7625`。

程式已使用這條 Eq.(6)、既有 mixed PSD 與 IWC normalization 做 18 組數值 mass-closure preflight，數值閉合與解析度收斂都通過。這證明「程式能依這套數學正確回算」，但**不等於物理 production 已經驗證完成**。

目前仍缺第二份獨立且可追溯的 Eq.(6) 數值佐證，所以 Dmax、habit、roughness、Ice τ 與 WINDY production Ice Optics 仍然保持 fail-close，不會猜值。

本版 QA regression：`846/846 PASS`。正式 FIELD baseline 仍是 `.10.19 FIELD PASS`；`.10.20.1` 需要再跑 FIELD CASE 才能決定是否升為 FIELD PASS。
