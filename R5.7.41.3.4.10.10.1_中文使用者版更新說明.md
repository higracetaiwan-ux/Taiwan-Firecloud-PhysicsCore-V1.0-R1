# R5.7.41.3.4.10.10.1 中文使用者版更新說明

這版的重點不是再讓 WINDY 等 PhysicsCore 算完，而是把 Ice Optics 做成可以獨立帶走的科學資料包。

之後架構是：PhysicsCore 建立/校準/驗證 Ice LUT；發布 Portable Package；WINDY 把 package 內建後自行運算。WINDY 不需要 PhysicsCore 在線，也不需要 Python 或 Streamlit。

目前已完成 portable contract、CSV/JSON LUT 格式、JavaScript/TypeScript evaluator、SHA256 manifest、跨語言驗證向量與 Node 驗證程式。沒有 authoritative calibrated LUT 時仍不會提供假係數。

`.10.10` 的 WINDY summary CSV/JSON 仍保留供 CASE 驗證，但未來不再是 WINDY 執行所必需。

Frozen Formation / Viewing / Twilight Glow 等科學規則完全未動。
