# R5.7.41.3.4.10.10 中文使用者版更新說明

這一版開始做你指定的 **Ice Cloud Spectral Optics LUT**，並同步建立 WINDY 外掛可讀取的共用資料格式。

六波段固定：`550 / 575 / 600 / 650 / 700 / 750 nm`。

程式已具備：

`ice particle source → Qext / A / V → k_ext [m²/kg] → IWP × k_ext → tau_ice(lambda) → transmission(lambda)`

但本版不附帶「猜出來的 LUT」。沒有安裝權威 calibrated LUT、沒有冰晶 r_eff/habit/roughness 時，正 IWP 的 tau 會維持 Missing，不會用固定 30 µm、RH 或雲量冒充。

WINDY 外掛新增共用輸出：

- `v1_windy_ice_optics_summary.csv`
- `windy_firecloud_ice_optics_summary_v1.json`

也可直接從 PhysicsCore UI 下載。

本版仍是 Phase 1 diagnostic/shared-export；不會改變既有火燒雲 Formation、Viewing、Glow 或攝影決策。
