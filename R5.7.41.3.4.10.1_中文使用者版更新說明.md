# R5.7.41.3.4.10.1 中文使用者版更新說明

這版沒有改火燒雲判斷規則，而是加速 Twilight Glow / Viewing 的觀測者路徑氣膠計算。

`.3.4.10` 真實 CASE 顯示 Glow 最大耗時項是 Observer Spectral Extinction。進一步檢查發現，同一條 CAMS 路線資料會被每個 Glow volume 重複整理 pressure levels。`.3.4.10.1` 改成每條路線只整理一次，之後直接重用數值陣列。

六波段、CAMS 原始資料、氣膠垂直插值、時間證據、最低層容忍、Missing 規則完全不變。離線用 TWS106 真實 CASE 做新舊演算法比對，輸出完全一致。

下一步只需跑一個 `.3.4.10.1` CASE，確認 `TWILIGHT_GLOW_COMPONENT_OBSERVER_SPECTRAL_EXTINCTION` 在實際 Streamlit runtime 中確實下降。
