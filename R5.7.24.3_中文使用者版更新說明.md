# R5.7.24.3 中文使用者版更新說明

這一版修的是「明明 CAMS 前面下載成功，後面分析卻全部顯示 Missing」的問題。

原因不是資料突然消失，而是一次分析跑了很多分鐘，剛好跨過 CAMS 新資料 cycle 的可用時間邊界。程式前半段用舊 cycle 下載成功，後半段重新判定時卻切到新 cycle，兩邊的快取 key 不同，因此後面找不到前面已下載的資料。

R5.7.24.3 從分析開始那一刻就把 provider cycle 判定時間固定下來。同一次分析從頭到尾都使用相同的 GFS / CAMS cycle 判定，不會因程式跑了 5～10 分鐘就自行換 cycle。

這不會把 Missing 假裝成有資料，也不會改變火燒雲物理結果。CAMS 若真的 timeout，仍會保留 Timeout / Missing；只是已經成功取得的 O3、氣膠等資料不會再因 cycle key 漂移而遺失。
