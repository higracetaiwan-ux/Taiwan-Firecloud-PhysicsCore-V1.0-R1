# R5.7.41.3.4.10.30.13 中文使用者版更新說明

這一版仍然不是修改火燒雲判斷公式，而是把 **RRTM_SW v2.5 的原始程式來源關係**再確認得更完整。

前一版已確認 AER 官方下載端點與歷史 distribution footprint；這一版進一步把 AER 官方 2004 source tree 和 external v2.5 distribution 的 Fortran 科學程式逐檔比較。

結果是：官方 scientific-source set 共 26 個 `.f` 檔案，其中 24 個在移除 CVS 自動展開資訊後整份完全一致。剩下兩個檔案也不是物理公式不同：`rrtatm.f` 只是把 3 行日期/時間輸出註解掉，`rrtm.f` 只是把 1 行輸入檔名改成 `input_rrtm_MLS`。

因此這一版可以正式確認 **scientific-source semantic equivalence**，也就是目前沒有發現這兩份 v2.5 source 在 RT 方程式、雲光學表、氣體光學表、光譜係數表或 band definition 上有科學內容差異。

但這仍然不代表兩個 repository 或 original tar.gz 是 byte-for-byte 相同，也沒有因此取得 original tarball hash，更沒有找回 Q. Fu 當年產生 band 24/25 table 的高解析 pre-averaging generator。

所以 Production Ice Optics 仍不解鎖，Step 3R 仍維持 blocked。
