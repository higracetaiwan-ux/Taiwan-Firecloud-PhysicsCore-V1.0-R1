# R5.7.38 中文使用者版更新說明

這一版主要不是修改火燒雲分數，而是解決 CAMS 已經算完資料、下載時卻突然卡住很久的問題。

之前真實 CASE 曾出現：CAMS 遠端工作大約 21 秒就完成，但下載時碰到 `502 Bad Gateway`，程式接著等待約 120 秒才重試，因此單一資料角色拖到約 153 秒。

R5.7.38 改成：遠端工作一旦顯示 `successful`，之後如果只是下載節點暫時失敗，就**沿用同一個 request ID**重新取得下載位置並短時間重試，不會重新向 CAMS 送出同一份工作。

預設最多嘗試 4 次，等待從 2 秒開始，最大等待 12 秒。即使伺服器回覆 `Retry-After: 120`，本程式也不會直接再卡 120 秒。

CASE 會增加 download attempts、retry count、backoff、download elapsed 等欄位，方便判斷時間到底花在 ADS queue、遠端運算，還是成功後下載。

下載網址本身不會保存到 CASE 或 journal，避免把暫時性的存取位置寫入診斷檔。

這一版**沒有改火燒雲 Formation、Viewing、Glow、六波段、10 m molecular tolerance、SSA/g 或 O₃ 物理**。


## 發行驗證

- Focused CAMS stateful + download recovery：**12/12 PASS**。
- Working-tree 完整 regression：**542/542 PASS**。
- FULL-CLEAN 預封包解壓後 regression：**542/542 PASS**。
- 正式封包不含 cache/pyc。
- R5.7.38 Field Validation：仍需新 CASE。
