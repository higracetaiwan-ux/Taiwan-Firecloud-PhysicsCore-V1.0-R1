# R5.7.41.3.4.10.4 中文使用者版更新說明

這一版不改火燒雲物理，只把「相同大氣狀態重複查同一份光譜係數」的 CPU 工作省掉。

`.10.3` 已把 Twilight Glow 的觀測者降水路徑從 14.309 秒降到 5.463 秒，Field PASS。之後最大的 Glow component 變成 Observer Spectral Extinction 8.533 秒。

分析 TWS134 真實 CASE 後發現，1092 個 Glow volume 雖有 8736 段 Gas RT，但 T/P 狀態大量跨方向/角度重複。以前每一段仍重新從同一 HITRAN Runtime LUT 找一次 O2/H2O/O3 六波段係數。

`.10.4` 現在只有在「LUT 內容、氣體、波長、T、P 完全相同」時重用先前的 sigma。光路長度、氣體密度、逐氣體乘法與累加仍每段照原本方式計算。

因此：**不是減少光譜、不是減少氣體、不是降低精度，而是避免重複查相同表格。**

下一個 Field CASE 要確認 Observer Spectral Extinction 是否從 8.533 秒明顯下降。
