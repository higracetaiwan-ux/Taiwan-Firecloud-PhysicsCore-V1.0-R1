# R5.7.33 中文使用者版更新說明

R5.7.32 真實 CASE 最後只剩 10 個 Glow `Scatter→Observer` Partial。R5.7.33
把它們拆成兩種完全不同的物理情況處理。

## 1. 六個 Gas / Rayleigh 邊界個案

100 km、散射高度 3.75 km 的第一段光線 midpoint 約 74.62 m，而最低真實
pressure-level profile 約 75.07 m，只差約 0.45 m。

R5.7.33 讓 Glow 的 Rayleigh 與 HITRAN gas-species 積分沿用既有 Gas RT 的
`<=10 m` 最低原生 pressure-level 邊界容忍。只允許使用最低的真實 native level，
超過 10 m 仍維持 Missing/Partial，不做外插。

## 2. 四個 Cloud 個案不能硬修成 Full

另外四個 100 km、7.75 km 個案，光線真的穿過一層 cloud fraction=0.1 的雲層，
但原生 condensate 明確為 0，因此 COT 無法確定，屬於
`DIRECT_EVIDENCE_CONFLICT / UNRESOLVED_CONFLICT`。

這不是程式漏資料，所以 R5.7.33 不會把它改成 clear，也不會用 cloud fraction 推 COT。
輸出會明確標示：

`GLOW_OBSERVER_CLOUD_DIRECT_EVIDENCE_CONFLICT_PRESERVED`

cloud tau 仍保持 Missing。

因此正確目標不是 1092/1092 Full，而是：

- molecular 100 km：156/156 resolved；
- 整體 observer extinction：舊 CASE forensic 預期 1088/1092 Full；
- 剩下 4/1092 是有證據衝突的 Partial，刻意保留。

這符合 `Missing ≠ Clear ≠ Zero`。
