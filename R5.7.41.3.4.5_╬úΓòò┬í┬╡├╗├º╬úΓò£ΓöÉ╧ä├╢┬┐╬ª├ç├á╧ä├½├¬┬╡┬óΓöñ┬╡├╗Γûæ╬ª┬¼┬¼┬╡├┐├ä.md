# R5.7.41.3.4.5 中文使用者版更新說明

這一版仍然是「減少重複工作」，不是改火燒雲判斷。

舊流程裡，Viewing 已經為每片候選雲計算過「雲→觀測者」路徑上的 blocker 與 conflict；Twilight Glow 後面卻會再追一次同樣的雲層幾何，只為判斷 Missing / direct conflict 的來源。

新版把這份 provenance 在 Viewing 同一次計算時保留下來，直接交給 Glow 使用，並讓兩個分支共用可安全重用的 route groups、gas contexts、COT / optical-truth lookup 與 cloud support geometry cache。

安全規則：只有來源資料是同一個 in-memory DataFrame object 才允許共用 context。只要來源物件不同，就重新建立，避免跨 CASE 或不同 run/lead 誤用舊 cache。

因此：

- 不會少算六波段；
- 不會放寬 Shadow eligibility；
- 不會改 Earth Shadow；
- 不會把 conflict 變成 clear；
- 不會把 Missing 當成 0；
- 舊 CASE 沒有新 handoff 欄位時仍能走原 fallback。

離線 H004 / TWS021 84-volume 測試中，Glow branch 從約 3.96 秒降到 2.04 秒，detail / summary 數值完全一致。這只是局部分支 benchmark，不代表整個 Online CASE 一定會快 1.95 倍。
