# PhysicsCore V1.0 — R5.7.41.3.4.10.30.20 使用者版更新說明

這一版先把 Band25「如何驗證 exact reconstruction」的工具做好，而不是猜 historical fine grid 或 solar weights。

最重要的進展：程式現在會自己把 Fu96 第一個 primary solar band 的公式重新算過 46 個 Dge 節點，再逐點和 archived RRTMG Band25 table 比對。這讓後續任何找到的 historical fine-grid / weighting candidate 都能放進同一套 deterministic harness 驗證，而不是靠人工看幾個數字。

同時修正一個 provenance 文件一致性問題：Step 3Q.17 已確定 co-albedo averaging 必須含 `beta_lambda` extinction weighting，但 `.19` 的 contract 文字欄位仍留著舊式 solar-only 字串。本版已統一修正，沒有改火燒雲 Formation / Viewing / Twilight Glow 科學規則。

目前仍不能宣稱 Band25 exact reproduce，更不能啟用 Production Ice Optics。真正還缺的是 historical fine spectral grid、interpolation realization、pre-averaging spectral samples、exact solar spectrum 與 within-band discrete weights。

最新 FIELD baseline 仍是 `.10.30.18.2 FIELD PASS`；`.10.30.20` 目前是 QA-complete 的 engineering baseline。
