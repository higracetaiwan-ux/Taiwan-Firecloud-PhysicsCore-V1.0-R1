# R5.7.41.3.4.10.24.1 中文使用者版更新說明

這一版不是改火燒雲物理，而是修正 Step 3K 證據檔「不同電腦最後幾位浮點數不同」的問題。

`.10.24` TWS100 FIELD CASE 的 Fu96 cross-check 已通過，但部分數字只差約十兆分之一兆（約 1e-16），仍會讓 CASE 與 release 檔案無法 byte-exact。`.10.24.1` 把 Step 3K 輸出固定成穩定有效位數，讓不同平台得到相同 evidence / contract。

計算本身沒有降低精度：只有保存到 evidence/contract 時 canonicalize；內部物理仍使用 double precision。

本版仍不會自動產生 Dmax、habit、roughness、production k_ext 或 tau_ice。
