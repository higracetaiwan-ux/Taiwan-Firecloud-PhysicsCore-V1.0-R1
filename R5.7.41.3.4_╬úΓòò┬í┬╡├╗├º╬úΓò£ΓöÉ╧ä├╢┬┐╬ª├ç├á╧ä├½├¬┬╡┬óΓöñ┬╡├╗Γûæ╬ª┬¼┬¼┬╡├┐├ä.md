# R5.7.41.3.4 中文使用者版更新說明

這一版專門處理「比較舊的 GFS 預報在 NOMADS 已經抓不到」的歷史回測問題。

原本程式只會向 NOMADS GRIB Filter 要 GFS 0.25° 原生雲微物理。當日期超出 NOMADS 線上窗口，例如 2026-08-30 回測實際需要的 2026-08-29 18Z f003，NOMADS 回 HTTP 403，程式只能把 CLWMR/ICMR 保持 Missing。

新版會先照舊嘗試 NOMADS；若失敗，**不換 GFS cycle，也不換 forecast hour**，而是到 NOAA 的 AWS GFS Open Data 找同一個檔案。程式先讀很小的 `.idx` 索引，再只下載我們需要的變數與 pressure levels 對應 byte ranges，因此不需要整包下載數百 MB 的全球 GRIB2。

主 GFS `pgrb2.0p25` 與 0–100 km Canvas 使用的 `pgrb2b.0p25` intermediate-level probe 都一起支援。

如果 AWS 上真的沒有該歷史物件或 native condensate message，結果仍會顯示 Missing，不會用 RH 或 Cloud Fraction 補造 COT。

這版不改火燒雲判定公式，只增加歷史資料的取得路徑。

## 驗證
Working-tree、Trial fresh-extract、Final fresh-extract 均為 604/604 PASS；1 個既有 pandas FutureWarning，非失敗。Release Gate CLOSED。
